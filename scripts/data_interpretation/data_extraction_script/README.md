# Running the script
To run the script use the following command. This will walk the directory tree and extract the necessary 
data into plots.

```commandline
python extract_fanout_metrics.py \
  --root /path/to/experiments/riak \
  --out  /path/to/experiments/_plots
```